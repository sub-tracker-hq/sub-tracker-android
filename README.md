# sub-tracker-android
Android app for Sub-tracker built with Kotlin and Jetpack Compose. Subscription tracking, bank linking, biometric auth, and FCM push notifications.

## Getting started

Open the project root in Android Studio and let it sync — this generates the
Gradle wrapper on first sync. Requires the Android SDK (compileSdk 34).
